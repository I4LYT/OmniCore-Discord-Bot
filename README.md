# OmniCore Discord Bot

## Building
just do `cargo build --release` inside of the project folder, the binary will be in `target/release/omnicore`. You do need Rust though, and if you're on windows, it will produce it with a .exe at the end. I discourage windows so use Docker or WSL